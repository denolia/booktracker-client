+++ remove redux
+++ I'd like to get rid of create react app
+++ switch to a simple webpack script instead
+++ Add a simple express server
+++ Add CSS modules config
+++ update libs

--- we are here ---

* remove bootstrap :|
* wrap all must-be-logged-in pages into redirect (after lib update)
* persist token somewhere (cookies)

configure nginx
add test step to CI
add deploy step to CI
